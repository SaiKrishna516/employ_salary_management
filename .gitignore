# Environment — never commit the real .env
.env

# Rails generated caches and runtime files — never commit these
backend/tmp/
backend/*.pid

# macOS metadata — everywhere
.DS_Store
**/.DS_Store

# Editor
.idea/
.vscode/
coverage/
